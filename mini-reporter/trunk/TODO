Pressing:
---------

* Find a way to restrict the width of the table to the width of the 
input cells.

* Add constraints on the input fields as a precaution to make sure the
preview looks well.

Long-term:
----------

* Convert to CGI::Application. (and HTML::Template ?)

* Create an administration page.

* Put on CPAN.

* Convert to Tangram.

