* Make sure the output stay validated as XHTML 1.0 Strict. (and validate)

* Validate the input of the form to display the correct record and to warn
on errors.

* Add some hints on what to put in the fields.

* Verify that there are no XSS exploits.

* Make sure newlines work as they should in some places and don't work 
(as they should) in others.

* Convert to CGI::Application. (and HTML::Template ?)

Long-term:
----------

* Create an administration page.

* Put on CPAN.

* Convert to Tangram.

